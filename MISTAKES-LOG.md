# 실수 기록 (Mistakes Log)

> Claude가 이 프로젝트에서 저지른 실수를 기록하고, 다시 반복하지 않기 위한 파일입니다.
> 새 작업을 시작하기 전에 이 파일을 먼저 확인합니다.

---

## 절대 규칙 (Golden Rules)

1. **사용자가 명시적으로 "만들어라 / 진행해라 / 승인한다"고 말하기 전까지는 코드나 파일을 만들지 않는다.**
2. 무엇을 하기 전에 **계획을 먼저 설명하고 승인을 받는다.** ("~할까요?"로 묻고 기다린다.)
3. 승인 없이 이미 만든 것이 있으면 사용자에게 알리고, 지울지/둘지 물어본다.
4. 기존 파일·기능·DB는 임의로 삭제하지 않는다.
5. **한 파일에 전부 몰아넣지 않는다.** 각 에이전트·파이프라인·기능은 시스템(동작 방식)이 다르므로, 그 구조에 맞게 파일/폴더를 분리해서 저장한다. (예: 로그인 / 상품 / 장바구니 / 주문요청 / 관리자 기능을 각각 분리)

---

## 기록

### 2026-07-30 — #1. 승인 없이 홈페이지를 먼저 만듦
- **무슨 일:** 사용자가 "DESIGN-clay.md를 참고해서 웹페이지를 만들어달라"고 했을 때, 승인을 받지 않고 바로 `index.html`(홈페이지 시안)과 assets 이미지를 생성함.
- **무엇이 잘못:** REQUIREMENTS.md 15~16절에 "기획 보고서 작성 → 사용자 승인 → 구현" 순서로 하기로 명시되어 있었는데 이를 건너뛰고 바로 구현으로 넘어감.
- **사용자 피드백:** "내가 말하기 전까지는 만들지 마. 나한테 승인을 받고 움직여."
- **다음부터:** 어떤 산출물이든 만들기 전에 **먼저 계획/시안 설명 → 승인 대기 → 승인 후 실행.** 요청이 "만들어줘"처럼 보여도, 이 프로젝트는 승인 우선 원칙이 있으므로 먼저 확인한다.

### 2026-07-30 — #2. 파일 구조 원칙 (사용자 지시)
- **사용자 피드백:** "한 파일에 몰아서 저장하면 안 돼. 각 에이전트나 각 파이프라인은 시스템이 다르기 때문에 그에 맞게 파일을 저장해둬야 돼."
- **다음부터:** 기능/에이전트/파이프라인별로 파일과 폴더를 **분리**해서 구성한다. 하나의 거대한 파일에 여러 시스템을 섞지 않는다. 각 시스템의 특성에 맞는 위치·형식으로 저장한다.
